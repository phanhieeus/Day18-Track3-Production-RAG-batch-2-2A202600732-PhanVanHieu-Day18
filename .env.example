# cp .env.example .env
# Required for M4 (RAGAS eval) and M5 (Enrichment LLM calls)
OPENAI_API_KEY=sk-...

# LLM provider OpenAI-compatible (vd ckey.vn). Để trống OPENAI_BASE_URL nếu dùng OpenAI gốc.
OPENAI_BASE_URL=https://api.xah.io/v1
LLM_MODEL=gpt-5.4-mini
